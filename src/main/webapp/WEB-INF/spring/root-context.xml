<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
	xsi:schemaLocation="http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring-1.2.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">
	<!-- Root Context: defines shared resources visible to all other web components -->
	<!-- 프로젝트 관련된 모든 정보(객체) 생성 파일 -->
	<!-- ★★★★★태그 내부에 주석문 사용 불가능하므로 주의! (태그 시작 이전 or 끝 이후에 사용)★★★★★ -->
	
	
	<!-- DataSource : DB 연결 정보 객체 / property는 set메서드와 동일 -->
	<bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="net.sf.log4jdbc.sql.jdbcapi.DriverSpy" />
		<property name="url" value="jdbc:log4jdbc:mysql://localhost:3306/springdb" />
		<property name="username" value="root" />
		<property name="password" value="1234" />
	</bean>
	<!-- DataSource : DB 연결 정보 객체 -->
	
	
	<!-- SqlSessionFactory : DB연결, MyBatis 설정, SQL 실행 -->
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="configLocation" value="classpath:/mybatis-config.xml" />
		<property name="mapperLocations" value="classpath:mappers/**/*Mapper.xml" />
	</bean>
	<!-- SqlSessionFactory : DB연결, MyBatis 설정, SQL 실행 -->
	
	
	<!-- SqlSessionTemplate : DB연결, MyBatis 설정, Mapper 설정(SQL 실행), DB종료 -->
	<bean id="sqlSession" class="org.mybatis.spring.SqlSessionTemplate" destroy-method="clearCache">
		<constructor-arg name="sqlSessionFactory" ref="sqlSessionFactory" />
	</bean>
	<!-- SqlSessionTemplate : DB연결, MyBatis 설정, Mapper 설정(SQL 실행), DB종료 -->
	
	
	<!-- 외부 객체 등록 -->
	<context:component-scan base-package="com.fooeating.persistence" />

<!-- 	<context:component-scan base-package="com.itwillbs.service" /> -->
	
</beans>